# Agent Instructions

当前项目还未正式公布，所以不要因为历史状态做兼容性，连数据库我们都可以直接清除，不会有遗留状态。记住，我们是没有任何兼容性负担的，所以项目中任何不再需要的东西都可以清理掉，始终保持当前项目在当前状态下的最优状态。不需要任何历史妥协。

## UI

- 任何和设计相关的内容需要先读取 `DESIGN.md` 来确保符合我们的设计规范，修改 `DESIGN.md` 必须由用户同意后才可以，并且更新后需要运行 `npx -y @google/design.md lint DESIGN.md`
- 遇到任何 ui 相关问题，必须运行 `bunx ui-skills start` 来选择合适的 skills 来解决问题
