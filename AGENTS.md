# Agent Instructions

当前项目还未正式公布，所以不要因为历史状态做兼容性，连数据库我们都可以直接清除，不会有遗留状态。记住，我们是没有任何兼容性负担的，所以项目中任何不再需要的东西都可以清理掉，始终保持当前项目在当前状态下的最优状态。不需要任何历史妥协。

## Agent skills

### Issue tracker

Issue 和 PRD 使用本仓库内的本地 Markdown 文件管理，位置在 `.scratch/`。详见 `docs/agents/issue-tracker.md`。

### Triage labels

本仓库使用默认的五类 triage 状态词汇。详见 `docs/agents/triage-labels.md`。

### Domain docs

本仓库使用 single-context 的领域文档布局。详见 `docs/agents/domain.md`。
