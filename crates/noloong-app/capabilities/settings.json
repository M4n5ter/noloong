{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings-window",
  "description": "Permissions required by the Noloong settings window.",
  "windows": ["settings"],
  "permissions": [
    "core:event:allow-emit-to",
    "core:window:allow-set-title"
  ]
}
